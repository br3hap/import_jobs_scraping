<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View job.portal.offer View List -->
    <record id="view_job_portal_offer_list" model="ir.ui.view">
        <field name="name">view.job.portal.offer.list</field>
        <field name="model">job.portal.offer</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View job.portal.offer form -->
    <record id="view_job_portal_offer_form" model="ir.ui.view">
        <field name="name">view.job.portal.offer.form</field>
        <field name="model">job.portal.offer</field>
        <field name="arch" type="xml">
            <form string="Job Portal Offer">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View job.portal.offer search -->
    <record id="view_job_portal_offer_search" model="ir.ui.view">
        <field name="name">view.job.portal.offer.search</field>
        <field name="model">job.portal.offer</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action job.portal.offer -->
    <record id="action_job_portal_offer" model="ir.actions.act_window">
        <field name="name">Job Portal Offer</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">job.portal.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Job Portal Offer.
            </p>
        </field>
    </record>

</odoo>
